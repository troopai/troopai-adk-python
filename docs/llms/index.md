(llms/index)=

# LLM Providers

The native and LiteLLM providers, reasoning / extended thinking, the LLM
retry policy, and the per-call middleware block around `LLM.acomplete()`.

```{toctree}
:maxdepth: 1

litellm
openai
reasoning
retry_policy
llm_middleware
```
