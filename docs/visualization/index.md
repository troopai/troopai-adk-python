(visualization/index)=

# Visualization

Render a constructed `Flow` or compiled `Graph` to a Mermaid flowchart
or a Graphviz DOT diagram from the framework's own topology data.

```{toctree}
:maxdepth: 1

visualization
```
