(references/api/runner)=

# `Runner`

```{eval-rst}
.. autoclass:: troopai.adk.run.Runner
   :members:
   :show-inheritance:
```

Runner profile classes are documented in {ref}`run/runner-profile`:
`RunnerProfile`, `AgentRunner`, `SwarmRunner`, `GraphRunner`, `TaskRunner`,
`TaskPipelineRunner`, `TaskGroupRunner`, and `FlowRunner`.
