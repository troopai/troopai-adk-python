(cli/index)=

# Command-Line Interface

The `troopai` CLI lets you run, chat with, validate, scaffold, and serve
agents directly from a terminal — no Python script required.

```{toctree}
:maxdepth: 2

cli
```
