(tools/index)=

# Tools

The tool system in depth — toolsets, tool middleware, runtime
dependencies, deferred loading, JIT context-aware tools, streaming
results, output trimming, and per-tool rate limiting.

```{toctree}
:maxdepth: 1

tools
toolsets
middleware
tool_dependencies
deferred_tool_loading
jit_context_aware
streaming_tool_results
tool_output_trimmer
tool_rate_limiting
```
