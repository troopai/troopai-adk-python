(deploy/index)=

# Deployment

Take a local agent to production: package it into a container, expose it over
HTTP, and ship it to any cloud target with `troopai deploy`.

```{toctree}
:maxdepth: 2

serving
container
kubernetes
gcp
aws
scaling
```
