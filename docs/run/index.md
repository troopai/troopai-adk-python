(run/index)=

# Run

Runner entry-points and run-time controls — the interactive demo REPL,
reusable runner profiles, the max-turn salvage handler, `RunResult` helpers,
`RunState` serialization, streaming cancellation, and the pre-LLM input filter.

```{toctree}
:maxdepth: 1

demo_loop
runner_profile
on_max_turns
run_result_helpers
runstate_serialization
streaming_cancel
call_model_input_filter
```
